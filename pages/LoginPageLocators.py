class LoginPageLocators:
    # Email input field on the login page
    email_field = 'ap_email_login'

    # Continue button after entering email
    continue_btn = '[id="continue"] [type="submit"]'

    # Password input field (appears after email is submitted)
    password_field = '[type="password"]'

    # Sign-in submit button after entering password
    signin_btn = '[id="signInSubmit"]'